/* eslint-disable no-unused-vars */
import React from "react";
import PropTypes from "prop-types";

const Access = (props) => {
  return (
    <div>
      <p>This is the access route</p>
    </div>
  );
};

Access.propTypes = {};

export default Access;
