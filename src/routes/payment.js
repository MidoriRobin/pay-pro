/* eslint-disable no-unused-vars */
import React from "react";
import PropTypes from "prop-types";

const Payment = (props) => {
  return (
    <div>
      <p>This is the payment route</p>
    </div>
  );
};

Payment.propTypes = {};

export default Payment;
