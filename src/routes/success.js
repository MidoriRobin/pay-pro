/* eslint-disable no-unused-vars */
import React from "react";
import PropTypes from "prop-types";

const Success = (props) => {
  return (
    <div>
      <p>This is the success route</p>
    </div>
  );
};

Success.propTypes = {};

export default Success;
